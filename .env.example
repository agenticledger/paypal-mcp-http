# No server-side credentials needed — Bearer passthrough model
# Clients pass their PayPal credentials as: Bearer <client_id>:<client_secret>
PORT=3100
SERVER_BASE_URL=https://paypalmcp.agenticledger.ai
